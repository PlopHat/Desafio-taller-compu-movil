import 'package:flutter/material.dart';

class PaginaDesafio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Desafío'),
      ),
      body: Center(
        child: Text('Página de Desafío'),
      ),
    );
  }
}